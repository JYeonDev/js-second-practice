export function random() {
  return Math.floor(Math.random() * 10);
}

export const user = {
  name: 'HEROPY',
  age: 85
}

export default 123
// export default 와 export를 같이 사용이 가능하다.
// export default와 export의 가장 큰 차이점은 
// export default는 값을 하나밖에 지정할수 없지만 export는 제한없이 지정할수 있다는 것이다.